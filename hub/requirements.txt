fastapi>=0.109.0
uvicorn[standard]>=0.27.0
SQLAlchemy>=2.0.0
pydantic>=2.5.0
python-multipart>=0.0.6
PyJWT>=2.8.0
google-auth>=2.28.0
requests>=2.31.0          # transport for google-auth id_token verification
python-dotenv>=1.0.0      # optional: load .env in dev

# ── Database driver — install the ONE matching DATABASE_URL ──
# sqlite needs nothing (built into Python) — fine for dev/small teams.
psycopg2-binary>=2.9.0    # PostgreSQL (recommended for production)
# PyMySQL>=1.1.0          # MySQL / MariaDB

# ── Object storage — ONLY if STORAGE_BACKEND=s3 ──
# Works with AWS S3, Cloudflare R2, Backblaze B2, any S3-compatible store.
boto3>=1.34.0
